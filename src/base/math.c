proc V2
v2(R32 x, R32 y)
{
  V2 result = {.x = x, .y = y};
  return(result);
}

proc V3
v3(R32 x, R32 y, R32 z)
{
  V3 result = {.x = x, .y = y, .z = z};
  return(result);
}

proc V4
v4(R32 x, R32 y, R32 z, R32 w)
{
  V4 result = {.x = x, .y = y, .z = z, .w = w};
  return(result);
}
